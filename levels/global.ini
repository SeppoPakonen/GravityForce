-- global.ini
-- Global init file
-- -> used when global_script in .lvl is set

dofile("./levels/libosd.sc")
init_remaining_time_osd1(10,10)

if (globals.network_game == 1) then
  init_base_labels()
end

dofile("./levels/libeff.sc")

if (globals.capture_the_flag == 1) then
  dofile("./levels/gm_ctf.sc")
end

if (globals.hunter_hunted == 1) then
  dofile("./levels/gm_hh.sc")
end  

if (globals.race == 1) then
  dofile("./levels/gm_race.sc")
end  

-- compatibility with older versions
function mod(a, b)
  return int_mod(a, b)
end
