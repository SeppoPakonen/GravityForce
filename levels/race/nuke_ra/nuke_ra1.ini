-- INI file
-- created by GS-Map-Editor v0.6

mybase = bases:add(  0, CARGO_BASE,  436, 1686 )

-- A

myob = objects:add(  0, OBJ_RACEPOLER,  307, 1378 )

myob = objects:add(  1, OBJ_RACEPOLEU,  621, 1595 )

myob = objects:add(  2, OBJ_RACEPOLER,  325,  716 )

myob = objects:add(  3, OBJ_RACEPOLEL,  808,  629 )

myob = objects:add(  4, OBJ_RACEPOLEU,  246,  433 )

myob = objects:add(  5, OBJ_RACEPOLEL,  418,  216 )

-- B

myob = objects:add_special(  0, SPOBJ_BUMPD,  160,   45 )

myob = objects:add_special(  1, SPOBJ_BUMPD,   42,   45 )

myob = objects:add_special(  2, SPOBJ_BUMPD,  101,   45 )

myob = objects:add_special(  3, SPOBJ_BUMPL,   41,   45 )

myob = objects:add_special(  4, SPOBJ_BUMPL,   41,  165 )

myob = objects:add_special(  5, SPOBJ_BUMPL,   41,  105 )

-- A

myob = objects:add(  6, OBJ_RACEPOLEU,  766,  494 )

myob = objects:add(  7, OBJ_RACEPOLED,  766,  328 )

myob = objects:add(  8, OBJ_RACEPOLED, 1568,  762 )

myob = objects:add(  9, OBJ_RACEPOLEU, 1570,  914 )

myob = objects:add( 10, OBJ_RACEPOLEU, 1484,  196 )

myob = objects:add( 11, OBJ_RACEPOLED, 1480,   46 )

-- C

-- SPOBJ_TELEPORT1, Parameter:                            zielteleporter
myob = objects:add_special(  6, SPOBJ_TELEPORT1, 1975,  111,             12 )

-- SPOBJ_TELEPORT1, Parameter:                            zielteleporter
myob = objects:add_special(  7, SPOBJ_TELEPORT1, 1977,  184,             12 )

-- SPOBJ_TELEPORT1, Parameter:                            zielteleporter
myob = objects:add_special(  8, SPOBJ_TELEPORT1, 1978,  261,             12 )

-- SPOBJ_TELEPORT1, Parameter:                            zielteleporter
myob = objects:add_special(  9, SPOBJ_TELEPORT1, 2134,  189,             12 )

-- SPOBJ_TELEPORT2, Parameter:                            zielteleporter
myob = objects:add_special( 10, SPOBJ_TELEPORT1, 2069,  162,             12 )  -- EDIT the last number (0): Target of teleporter

-- SPOBJ_TELEPORT2, Parameter:                            zielteleporter
myob = objects:add_special( 11, SPOBJ_TELEPORT1, 2073,  222,             12 )  -- EDIT the last number (0): Target of teleporter

-- SPOBJ_TELEPORT1, Parameter:                            zielteleporter
myob = objects:add_special( 12, SPOBJ_TELEPORT1,   27, 1478,             12 )
myob:set_invisible(1)
 
-- D

-- E_TOWER_SINGLE, Parameter:                       signatur, richtung, feuerfrequenz, akt.radius, schussgeschwindigkeit, schussstaerke
myen = enemies:add(  1, E_TOWER_SINGLE,  351, 1683,        0,     D_UP,            60,       1000,                     2,             1 )

-- E_TOWER_SINGLE, Parameter:                       signatur, richtung, feuerfrequenz, akt.radius, schussgeschwindigkeit, schussstaerke
myen = enemies:add(  2, E_TOWER_SINGLE,  368, 1684,        0,     D_UP,            60,       1000,                     2,             1 )

-- E_TOWER_SINGLE, Parameter:                       signatur, richtung, feuerfrequenz, akt.radius, schussgeschwindigkeit, schussstaerke
myen = enemies:add(  3, E_TOWER_SINGLE,  389, 1684,        0,     D_UP,            60,       1000,                     2,             1 )

-- E_TOWER_SINGLE, Parameter:                       signatur, richtung, feuerfrequenz, akt.radius, schussgeschwindigkeit, schussstaerke
myen = enemies:add(  4, E_TOWER_SINGLE,  528, 1687,        0,     D_UP,            60,       1000,                     2,             1 )

-- E_TOWER_SINGLE, Parameter:                       signatur, richtung, feuerfrequenz, akt.radius, schussgeschwindigkeit, schussstaerke
myen = enemies:add(  5, E_TOWER_SINGLE,  545, 1684,        0,     D_UP,            60,       1000,                     2,             1 )

-- E_TOWER_SINGLE, Parameter:                       signatur, richtung, feuerfrequenz, akt.radius, schussgeschwindigkeit, schussstaerke
myen = enemies:add(  6, E_TOWER_SINGLE,  561, 1682,        0,     D_UP,            60,       1000,                     2,             1 )

-- E_TOWER_SINGLE, Parameter:                       signatur, richtung, feuerfrequenz, akt.radius, schussgeschwindigkeit, schussstaerke
myen = enemies:add(  7, E_TOWER_SINGLE,  404, 1104,        0,   D_DOWN,           240,       5000,                     3,             1 )

-- E_TOWER_SINGLE, Parameter:                       signatur, richtung, feuerfrequenz, akt.radius, schussgeschwindigkeit, schussstaerke
myen = enemies:add(  8, E_TOWER_SINGLE,  420, 1104,        0,   D_DOWN,           200,       5000,                     3,             1 )

-- E_TOWER_SINGLE, Parameter:                       signatur, richtung, feuerfrequenz, akt.radius, schussgeschwindigkeit, schussstaerke
myen = enemies:add(  9, E_TOWER_SINGLE,  435, 1098,        0,   D_DOWN,           240,       5000,                     3,             1 )

-- E_TOWER_SINGLE, Parameter:                       signatur, richtung, feuerfrequenz, akt.radius, schussgeschwindigkeit, schussstaerke
myen = enemies:add( 10, E_TOWER_SINGLE,  448, 1092,        0,   D_DOWN,           240,       5000,                     3,             1 )

-- E_TOWER_SINGLE, Parameter:                       signatur, richtung, feuerfrequenz, akt.radius, schussgeschwindigkeit, schussstaerke
myen = enemies:add( 11, E_TOWER_SINGLE,  462, 1089,        0,   D_DOWN,           240,       5000,                     3,             1 )

-- E_TOWER_SINGLE, Parameter:                       signatur, richtung, feuerfrequenz, akt.radius, schussgeschwindigkeit, schussstaerke
myen = enemies:add( 12, E_TOWER_SINGLE,  476, 1054,        0,   D_DOWN,           240,       5000,                     3,             1 )

-- E_TOWER_SINGLE, Parameter:                       signatur, richtung, feuerfrequenz, akt.radius, schussgeschwindigkeit, schussstaerke
myen = enemies:add( 13, E_TOWER_SINGLE,  490, 1050,        0,   D_DOWN,           240,       5000,                     3,             1 )

-- E_TOWER_SINGLE, Parameter:                       signatur, richtung, feuerfrequenz, akt.radius, schussgeschwindigkeit, schussstaerke
myen = enemies:add( 14, E_TOWER_SINGLE,  504, 1043,        0,   D_DOWN,           240,       5000,                     3,             1 )

-- E_TOWER_SINGLE, Parameter:                       signatur, richtung, feuerfrequenz, akt.radius, schussgeschwindigkeit, schussstaerke
myen = enemies:add( 15, E_TOWER_SINGLE,  514, 1036,        0,   D_DOWN,           240,       5000,                     3,             1 )

-- E

myob = objects:add( 12, OBJ_TUNNEL1,  696, 1058 )

myob = objects:add( 13, OBJ_MAN1,  700, 1053 )

myob = objects:add( 14, OBJ_MAN1,  708, 1053 )

myob = objects:add( 15, OBJ_TUNNEL1,  199,  384 )

myob = objects:add( 16, OBJ_MAN1,  204,  379 )

myob = objects:add( 17, OBJ_MAN1,  211,  379 )

myob = objects:add( 18, OBJ_TUNNEL1, 1579,  620 )

myob = objects:add( 19, OBJ_MAN1, 1583,  614 )

myob = objects:add( 20, OBJ_MAN1, 1591,  614 )

myob = objects:add( 21, OBJ_TUNNEL1, 1100,  535 )

myob = objects:add( 22, OBJ_MAN1, 1105,  530 )

myob = objects:add( 23, OBJ_MAN1, 1112,  531 )

-- F

globals.race_laps = 2
