-- CG-Race 12
-- INI-Datei
-- created by GS-Map-Editor v. 0.7
-- und Frapela
-- 24. November 2004

runden_anz_fuer_highscore = 4
globals.extra_amount      = 0
local n
for n = 1, globals.players do
  player[n]:set_extra_power(0)
  player[n]:set_extra_recharge(0)
  player[n]:set_fuel_dec(0)
end

bases:add( 0, PLAYER_BASE,  959, 1388 )
if globals.players >= 3 then
  bases:add( 1, PLAYER_BASE,  863, 1388 )
end
if globals.players >= 5 then
  bases:add( 2, PLAYER_BASE, 1055, 1388 )
end

objects:add( 0, OBJ_RACEPOLED, 1145, 1104 )
objects:add( 1, OBJ_RACEPOLEU, 1145, 1382 )
objects:add( 2, OBJ_RACEPOLEL, 1334,  979 )
objects:add( 3, OBJ_RACEPOLER, 1199,  979 )
objects:add( 4, OBJ_RACEPOLED,  982,  623 )
objects:add( 5, OBJ_RACEPOLEU,  982,  758 )
objects:add( 6, OBJ_RACEPOLEL,  758,  979 )
objects:add( 7, OBJ_RACEPOLER,  623,  979 )
objects:add( 8, OBJ_RACEPOLED,  815, 1104 )
objects:add( 9, OBJ_RACEPOLEU,  815, 1382 )

if globals.activate_extra == 1 then
  dofile(LEVEL_DIR .. "/cgrace12.wp")
  dofile(LEVEL_DIR .. "/cgracexx.cg")
else
  globals.race_laps = runden_anz_fuer_highscore
end
